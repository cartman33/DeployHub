# DeployHub 프론트엔드 변경사항 (2026-08-21)

## 1. 전달 요약

메인버전 페이징, 배포자/개발자 화면 UX 개선, JOB 상세 및 재시도 흐름 보강을 중심으로 진행했습니다.

- 작업 브랜치: `feat/minpyo`
- 오늘 생성한 로컬 커밋: `070e27c feat: improve version and package management flows`
- 현재 상태: `origin/feat/minpyo` 기준 `ahead 1, behind 2`
- 추가 미커밋 변경: 5개 소스 파일 수정 및 공용 드롭다운 1개 추가
- 검증: `git diff --check`, `npm run build` 통과

> 원격 브랜치의 커밋 2개가 로컬에 없으므로 바로 push하지 말고, 커밋 후 원격 변경과 비교·통합해야 합니다.

## 2. 공통 영역

### 메인버전 목록 페이징

- 메인버전을 한 번에 20개씩 조회하도록 변경했습니다.
- 서버의 `PageResponse`에서 `items`, `page`, `size`, `totalCount`를 사용합니다.
- 기존 목록 뒤에 다음 페이지를 누적하고, 버전명이 같은 항목은 중복 제거합니다.
- 서버가 내려준 정렬 순서를 유지해 페이지 경계에서 정렬이 뒤섞이지 않게 했습니다.
- 배포자 모드와 개발자 모드가 공용 `VersionDropdown`을 사용합니다.
- 드롭다운 최하단에 도달하면 다음 20개를 자동으로 불러옵니다.
- 로딩 중 중복 요청과 마지막 페이지 이후 요청을 차단합니다.

### 화면 모드 유지

- 배포자 모드, 개발자 모드, JOB 관리 중 마지막으로 열었던 화면을 `localStorage`에 저장합니다.
- 브라우저 새로고침 후에도 기존 화면을 유지합니다.
- 상단 메뉴에서 배포자 모드와 JOB 관리를 한 그룹으로, 개발자 모드를 별도 그룹으로 구분했습니다.
- 상단 메뉴, Deploy Hub 제목과 각 모드 섹션의 장식용 이모지·아이콘을 제거하고 텍스트 중심으로 통일했습니다.

## 3. 배포자 모드

### 버전 선택 및 비교

- 화면 진입 시 좌우 버전을 자동 선택하지 않고 사용자가 직접 선택하도록 변경했습니다.
- 좌측 현재 버전과 우측 업데이트 버전의 검색 결과를 서로 분리했습니다.
- 한쪽 검색이 다른 쪽 선택값을 바꾸지 않습니다.
- 업데이트 버전이 현재 버전보다 낮거나 같은 잘못된 비교 범위를 선택하지 못하게 했습니다.
- 버전 간 차이는 긴 세로 목록 대신 메인버전 한 개당 한 페이지로 표시합니다.
- 이전/다음 버튼으로 비교 범위 안의 버전을 이동합니다.
- 좌우 테이블의 헤더, 열 너비, 행 높이와 빈 화면 디자인을 맞췄습니다.
- 카드 경계, 배경색과 그림자를 보강해 영역 구분을 명확하게 했습니다.

### 패키징 선택 조건

- 배포자 표에 `VERSION` 열을 추가했습니다.
- IMAGE TAG가 없으면 임의의 `APP:VERSION` 값을 만들지 않고 `없음`으로 표시합니다.
- IMAGE TAG가 없는 EXT는 체크박스를 비활성화합니다.
- 전체 선택에서도 EXT 등 IMAGE TAG가 없는 항목은 제외합니다.
- 비교 대상에 PENDING 항목이 하나라도 있으면 개별/전체 선택과 패키징 시작을 차단합니다.
- 차단 사유를 사용자에게 알림으로 표시합니다.
- 패키징 시작 실패 시 백엔드 오류의 `message`와 `detail`을 장바구니 아래 오류 영역에 함께 표시합니다.
- 좌우 버전을 변경하면 이전 선택 버전에 속한 패키징 오류를 즉시 제거합니다.
- 장바구니는 배포자 화면 상단으로 이동했습니다.
- 업데이트 버전을 바꾸면 이전 버전의 선택 항목이 섞이지 않도록 장바구니를 초기화합니다.

### 화면 정리

- 배포 결과 및 URL 영역을 배포자 모드에서 제거하고 JOB 관리 상세로 이동했습니다.
- 작은 화면에서도 STATUS 글자가 숨지 않도록 상태 영역 너비와 버튼 배치를 조정했습니다.
- 중복 안내 문구와 IMAGE TAG의 `(선택)` 문구를 정리했습니다.
- 상태 명칭은 `UPDATED`, `PENDING`, `UNCHANGED`로 일관되게 표시합니다.

## 4. 개발자 모드

### 메인버전 관리 UX

- 신규등록과 기존 버전 관리 흐름을 화면에서 명확하게 구분했습니다.
- 관리할 메인버전 드롭다운에도 20개 단위 무한 스크롤 페이징을 적용했습니다.
- 같은 날짜의 기본 버전과 `-1`, `-2` 버전을 서로 다른 버전으로 처리합니다.
- SQL SCRIPT와 RELEASE NOTE를 하나의 배포 문서 영역으로 묶었습니다.
- 기존 메인버전의 SQL/Release Note를 함께 수정하는 `배포 문서 저장` 버튼을 제공합니다.
- 빈 문자열도 수정 API에 전달해 기존 SQL/Release Note를 삭제할 수 있습니다.

### APP별 정보

- APP별 정보 오른쪽에 서버 최신 데이터를 다시 받는 새로고침 버튼을 추가했습니다. 
- 미저장 변경이 있으면 새로고침 전에 확인합니다.
- 서버에서 받은 원본값과 현재 입력값을 비교해 변경 여부를 계산합니다.
- UNCHANGED 행을 수정하면 자동으로 UPDATED가 됩니다.
- 입력값과 순서를 원래대로 되돌리면 다시 UNCHANGED가 됩니다.
- 드래그 순서 변경도 저장 대상 변경으로 판단합니다.
- 저장 버튼을 오른쪽 끝에 배치해 VERSION, IMAGE TAG, NOTE 공간을 넓혔습니다.
- 저장된 행은 서버의 실제 ID로 삭제하고, 아직 저장하지 않은 행은 화면에서만 제거합니다.

### EXT 처리

- APP 이름이 EXT이면 IMAGE TAG 입력을 비활성화합니다.
- EXT 저장 요청은 `imageTags: []`로 전송합니다.
- VERSION, NOTE, STATUS는 다른 APP과 동일하게 관리합니다.

## 5. JOB 관리

### 목록 및 상세 UI

- 메뉴와 제목을 `JOB 관리`, `패키징 JOB 관리`로 정리했습니다.
- 목록 열을 `메인버전 → 진행률 → 상태 → 일시 → 결과 폴더 → 작업` 순서로 배치했습니다.
- 진행률의 퍼센트와 완료/전체 건수를 가까이 정렬했습니다.
- 가장 오른쪽에 작은 새로고침 버튼을 추가했습니다.
- 새로고침 시 JOB 목록뿐 아니라 현재 펼쳐진 패키징 항목, STATUS, 실패 정보와 결과 URL도 함께 다시 조회합니다.
- JOB 행을 클릭하면 패키징 항목, 결과 폴더 URL, 항목별 파일 URL을 펼쳐서 확인할 수 있습니다.
- URL 전체 복사 기능을 추가했습니다.
- `deletedAt`이 있으면 삭제된 JOB으로 표시하고 결과 폴더를 만료 상태로 표시합니다.
- 진행 중인 JOB은 패키지 삭제 버튼을 비활성화하고 DONE/FAILED만 삭제를 허용합니다.

### 오류 정보 및 재시도

- 항목별 STATUS, 실패 단계, 오류 코드, 파일 크기, 재시도 횟수와 오류 내용을 표시합니다.
- 실패 단계 필드가 없는 현재 API에서는 ErrorCode 범위를 이용해 다운로드/업로드/검증/외부 연동 단계를 추론합니다.
- FAILED 항목의 IMAGE TAG만 중복 제거해 `force: false`로 부분 재시도합니다.
- digest 불일치 `E-0603`은 같은 요청을 반복해도 해결되지 않아 자동 재시도에서 제외합니다.
- 재시도 응답이 `409 + E-0703(WORK_DIR_LOST)`일 때만 사용자 확인 후 `imageTags: [], force: true`로 전체 재수집합니다.
- 그 외 409 응답을 강제 재시도로 잘못 처리하지 않습니다.

## 6. 백엔드 확인 필요 사항

### FAILED JOB의 오류 데이터 누락

`GET /api/package-jobs/2026.08.20` 실제 응답을 확인한 결과:

- JOB 상태는 `FAILED`
- 항목은 `DOWNLOADED` 7개, `PENDING` 2개
- 모든 항목의 `errorMessage`는 `null`
- `errorCode`, `failureStage` 필드는 없음

이 상태에서는 프론트가 정확한 실패 항목, 실패 단계와 오류 코드를 판단할 수 없습니다. 현재 화면은 이를 숨기지 않고 다음처럼 표시합니다.

- FAILED JOB인데 오류 정보가 없다는 안내
- 미완료 PENDING 항목의 실패 단계: `미확인`
- 오류 코드: `미제공`
- 오류 내용: `JOB 실패 후 미완료 · 상세 오류 정보 미제공`

백엔드 요청 권장사항:

1. JOB 실패 시 실제 실패 항목의 `status`를 `FAILED`로 내려주기
2. 항목 또는 JOB에 `errorCode`, `errorMessage` 제공
3. 가능하면 추론이 필요 없도록 `failureStage` 제공
4. 재시도 API가 사용할 실패 IMAGE TAG를 명확하게 식별할 수 있게 응답 계약 확정

## 7. 코드리뷰용 주석

자명한 JSX나 단순 상태 선언마다 주석을 붙이지 않고, 리뷰어가 판단 근거를 알아야 하는 로직에 집중했습니다.

- 서버 페이징 순서 유지와 중복 제거 이유
- 좌우 검색 상태 분리와 선택 버전 추가 적재 이유
- 버전 비교 범위 및 페이지 계산 기준
- 상세 API 지연 조회와 중복 요청/경쟁 조건 방지
- PENDING, EXT, IMAGE TAG에 따른 패키징 차단 조건
- 개발자 모드 원본값 비교와 UPDATED/UNCHANGED 자동 복원 기준
- 서버 ID를 사용한 삭제와 EXT 저장 예외
- JOB 오류 단계 추론의 한계
- 실패 항목 부분 재시도, E-0603 제외, E-0703 강제 재시도 조건

## 8. 리뷰 및 테스트 체크리스트

1. 새로고침 후 현재 모드가 유지되는지 확인
2. 배포자/개발자 버전 드롭다운에서 최초 20개와 하단 추가 로딩 확인
3. 좌우 버전 검색이 서로의 목록과 선택값을 변경하지 않는지 확인
4. 낮은 업데이트 버전 선택이 차단되는지 확인
5. 버전별 비교 페이지 이전/다음 동작 확인
6. EXT 및 PENDING 항목 선택 차단 확인
7. 개발자 APP 입력 후 UPDATED, 원복 후 UNCHANGED 전환 확인
8. APP 정보 새로고침 시 미저장 변경 경고 확인
9. JOB 행 상세 열기, URL 복사와 삭제 상태 표시 확인
10. 실패 항목 부분 재시도 및 E-0703 강제 재시도 확인
11. `npm run build` 실행
